<resources>
    <!-- Base application theme. -->
    <style name="Theme.Login" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/status_bar</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white_two</item>
        <item name="colorSecondaryVariant">@color/text_description</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">@color/status_bar</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Main" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/bottom_sheet_btn</item>
        <item name="android:statusBarColor">@color/toolbar_bg</item>
        <item name="colorAccent">@color/bottom_sheet_btn</item>
    </style>

</resources>